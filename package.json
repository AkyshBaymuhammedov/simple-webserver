{
  "name": "service-b",
  "version": "1.0.0",
  "description": "A simple REST API with single endpoint which responds with 200 success for any GET requests",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Akysh Baymuhammedov",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
